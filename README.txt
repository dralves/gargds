#
# addresses can be hostname or ip (e.g. localhost or 120.0.1.4)
# Running a server, e.g.:
# java -cp class-ds-assignment-1.jar edu.ut.dsi.tickets.ServerMain localhost 3000
#
# Running a client:
# java -cp class-ds-assignment-1.jar edu.ut.dsi.tickets.ClientMain localhost 3000
#
#
